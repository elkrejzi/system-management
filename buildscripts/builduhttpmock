#!/bin/bash -e

export PKGNAME=uhttpmock
export PKGVER=0.5.0
export PKGTAR=${PKGNAME}-${PKGVER}.tar.xz
export PKGURL="https://tecnocode.co.uk/downloads/uhttpmock/${PKGTAR}"
export DEST=/binary/gnome/${PKGNAME}-${PKGVER}
export MAKE_JOBS_FLAGS="-j4"

. $(dirname $0)/master.sh
